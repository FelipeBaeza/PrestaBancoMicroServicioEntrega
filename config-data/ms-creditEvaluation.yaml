server:
  port: 8092
spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/ms-creditEvaluation
    username: postgres
    password: felipeb222
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/