server:
  port: 8095
spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/ms-totalCosts
    username: postgres
    password: felipeb222
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/